<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CineWave - Edit Title</title>
  <link rel="icon" type="image/png" href="/favicon.png?v=2">
  <link rel="stylesheet" href="/styles/style.css">
</head>
<body>

  <header>
    <h1>🎬 CineWave</h1>
    <p>Update entry details</p>
  </header>

  <div class="form-container">
    <form action="/edit/<%= item.id %>" method="POST" class="add-book-form">
      <h2>Edit Entry</h2>

      <div class="form-group">
        <label for="title">Title</label>
        <input type="text" id="title" name="title" value="<%= item.title %>" required>
      </div>

      <div class="form-row">
        <div class="form-group">
          <label for="type">Media Type</label>
          <select id="type" name="type" required>
            <option value="Movie" <%= item.type === 'Movie' ? 'selected' : '' %>>Movie</option>
            <option value="Series" <%= item.type === 'Series' ? 'selected' : '' %>>TV Series</option>
          </select>
        </div>

        <div class="form-group">
          <label for="category">Genre</label>
          <input type="text" id="category" name="category" value="<%= item.category %>" required>
        </div>
      </div>

      <div class="form-row">
        <div class="form-group">
          <label for="status">Watch Status</label>
          <select id="status" name="status" required>
            <option value="Watched" <%= item.status === 'Watched' ? 'selected' : '' %>>Watched</option>
            <option value="Not Watched" <%= item.status === 'Not Watched' ? 'selected' : '' %>>Not Watched</option>
          </select>
        </div>

        <div class="form-group">
          <label for="rating">Rating</label>
          <select id="rating" name="rating">
            <option value="5" <%= item.rating === 5 ? 'selected' : '' %>>⭐⭐⭐⭐⭐ (5/5)</option>
            <option value="4" <%= item.rating === 4 ? 'selected' : '' %>>⭐⭐⭐⭐ (4/5)</option>
            <option value="3" <%= item.rating === 3 ? 'selected' : '' %>>⭐⭐⭐ (3/5)</option>
            <option value="2" <%= item.rating === 2 ? 'selected' : '' %>>⭐⭐ (2/5)</option>
            <option value="1" <%= item.rating === 1 ? 'selected' : '' %>>⭐ (1/5)</option>
          </select>
        </div>
      </div>

      <div class="form-group">
        <label for="notes">Notes / Review</label>
        <textarea id="notes" name="notes" rows="3"><%= item.notes %></textarea>
      </div>

      <div class="form-actions">
        <a href="/" class="btn-cancel">Cancel</a>
        <button type="submit" class="btn-submit">Update Entry</button>
      </div>
    </form>
  </div>

</body>
</html>